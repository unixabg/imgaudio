# This file marks `lenses/` as a Python package so `imgaudio.py` can import
# individual lens modules. Lenses themselves should NOT be added here —
# put each lens in its own .py file (e.g. lenses/edges.py).
